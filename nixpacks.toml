[phases.setup]
nixPkgs = ["nodejs_24", "bun"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node build"

[variables]
NODE_ENV = "production"
